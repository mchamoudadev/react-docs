const ShowDate = ({ year, month, age }) => {
	// destructuring

	return (
		<div>
			<h1>{greeting}</h1>
			<h1>Hello World updated {year}</h1>
			<h1>current month is {month}</h1>
			<h1> {2 + 2}</h1>
		</div>
	);
};

export default ShowDate;
